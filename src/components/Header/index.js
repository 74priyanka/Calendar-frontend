import { StyledHeader } from "./styles";

const Header = () => {
  return (
    <StyledHeader className="add-event-form">
      <h2 className="app-header">My Event Calendar</h2>
    </StyledHeader>
  );
};

export default Header;
